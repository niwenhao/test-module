import * as React from "react"
import { BrowserRouter, Route, Link } from "react-router-dom"

export class HistMgr extends React.Component {
  render(): React.ReactNode {
    return (
      <div>
        <p>It is HistMgr</p>
      </div>
    )
  }
}
