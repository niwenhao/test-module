import * as React from "react"
import { BrowserRouter, Route, Link } from "react-router-dom"

export class ApiMgr extends React.Component {
  render(): React.ReactNode {
    return (
      <div>
        <p>It is ApiMgr</p>
      </div>
    )
  }
}
